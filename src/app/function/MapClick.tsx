'use client';

import { useEffect, useState } from "react";

const MapClick = (props:{num:number;}) => {
    const [resultData, setResultData] = useState<number>(0);

    return (
        <div>
        </div>
    )
}

export default MapClick;