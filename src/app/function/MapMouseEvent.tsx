'use client';

import { useEffect, useState } from "react";

const MapMouseClick = (props:{num:number;}) => {
    const [resultData, setResultData] = useState<number>(0);

    return (
        <div>
        </div>
    )
}

const MapMouseOver = (props:{num:number;}) => {
    const [resultData, setResultData] = useState<number>(0);

    return (
        <div>
        </div>
    )
}

const MapMouseOut = (props:{num:number;}) => {
    const [resultData, setResultData] = useState<number>(0);

    return (
        <div>
        </div>
    )
}

export default MapMouseOver;